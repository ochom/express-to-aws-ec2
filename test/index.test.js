import app from "../server.js";
import assert from "assert";

describe("server.js", () => {
  describe("happy got users", () => {
    it("should return an array of 3 items", () => {
      assert.equal(1, 1);
    });
  });
});
